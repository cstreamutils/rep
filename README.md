Rep some text, all the repping
===============================
Find a regex in a stream and replace it with the provided text.

## Usage:
`echo 'hello world' | rep hello stuff` => `stuff world`

## Install:
`make && make install`
